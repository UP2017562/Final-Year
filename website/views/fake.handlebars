<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fake Page</title>
    <link rel="stylesheet" href="/css/fakepage.css">

    {{#if preferences}}
    <style>
        body {
            font-family: {{preferences.font_style}}, sans-serif;
            font-size: {{preferences.font_size}}px;
            color: {{preferences.font_colour}};
            {{#if preferences.colour_contrast}}background-color: black;{{/if}}
        }

        /* CSS logic for image and paragraph */
        {{#if preferences.images_to_text}}
        img {
            display: none; /* Hide the image */
        }
        p.alt-text {
            display: block; /* Show the paragraph */
            font-size: 14px;
            color: red; /* Style the alt text */
        }
        {{else}}
        img {
            display: block; /* Show the image */
        }
        p.alt-text {
            display: none; /* Hide the paragraph */
        }
        {{/if}}
    </style>
    {{/if}}
</head>
<body>
    <h1>Welcome to the Fake Page</h1>
    <p>This page dynamically applies your accessibilitydsa dasd  preferences.</p>
    <p>Here is an image of a beaver:</p>



    <!-- Image and associated paragraph -->
    <img id="beaver-image" src="/img/beaver.png" alt="beaver Image">
    <p id="beaver-alt-text" class="alt-text">beaver Image</p>
    
</body>
</html>

